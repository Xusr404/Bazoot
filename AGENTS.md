# Bazoot scope

`Bazoot/` is the active application for this repository.

- Make implementation changes only within this directory unless the user explicitly directs otherwise.
- Run builds, tests, and graph commands from `Bazoot/`.
- Do not make changes to the legacy sibling `Bazoot/` application.
