// Engine phase timings in seconds, shared so the client demo mirrors game pacing.
export const ENGINE_TIMING = {
  /** quiz subject title shown before the countdown square */
  startTitleSeconds: 3,
  /** 3-2-1 rotating-square countdown before the first question */
  startCountdownSeconds: 3,
  /** "Question #N" 3D card before the question text appears */
  preparedSeconds: 2,
}
